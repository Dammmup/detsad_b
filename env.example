# ===== КОНФИГУРАЦИЯ ДЛЯ PRODUCTION =====

# Основные настройки
NODE_ENV=production
PORT=8080

# MongoDB - используйте ваш реальный connection string
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/test?retryWrites=true&w=majority&appName=YourApp

# Альтернативные базы данных для специфических коллекций (необязательно, можно использовать MONGO_URI для всех)
MEDICAL_MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/medician_journals?retryWrites=true&w=majority&appName=YourApp
FOOD_MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/food_journals?retryWrites=true&w=majority&appName=YourApp

# JWT Secret - КРИТИЧЕСКИ ВАЖНО! Сгенерируйте случайную строку для production
# Используйте: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-REPLACE-WITH-RANDOM-64-CHARS

# ===== ИНСТРУКЦИИ ДЛЯ RENDER.COM =====
# 1. Скопируйте эти переменные в Render Dashboard
# 2. Settings → Environment Variables
# 3. Замените значения на реальные
# 4. Сохраните и перезапустите сервис

# ===== БЕЗОПАСНОСТЬ =====
# НИКОГДА не коммитьте реальные значения в Git!
# Используйте этот файл только как шаблон


# CORS настройки (через запятую, без пробелов)
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com,https://another-domain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Firebase Configuration (optional, for push notifications)
# To configure Firebase, set FIREBASE_SERVICE_ACCOUNT with the JSON content of your service account key
# FIREBASE_SERVICE_ACCOUNT={"type": "service_account", "project_id": "...", "private_key_id": "...", ...}
# Alternatively, you can set GOOGLE_APPLICATION_CREDENTIALS with the path to your service account file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
